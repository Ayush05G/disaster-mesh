{
  "name": "aether-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Aether mesh dashboard (ROADMAP Phase 4). Local-only: reads the D4 ledger service on localhost:8700, all assets vendored, zero external requests at runtime (Critical Rule 1 — enforced by scripts/audit-offline.js at build time).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/audit-offline.js",
    "preview": "vite preview",
    "lint": "eslint ."
  },
  "dependencies": {
    "maplibre-gl": "6.0.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.0.4",
    "eslint": "^10.8.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "vite": "8.1.5"
  }
}
